import cs1.SimpleURLReader;
/**
 * CS102-Section 1 - Lab 2
 * @author Mehmet Kaan Örnek
 * @version 20.10.2020
 */ 
public class MySimpleURLReader extends SimpleURLReader
{
   private String givenUrl;
   
   public MySimpleURLReader( String url )
   {
      super( url );
      givenUrl = url;
   }
   
   public String getURL(){
      return givenUrl;
   }
   public String getName(){
      int index = 0;
      
      for( int i = givenUrl.length()-1; i > 0; i-- ){
         if( givenUrl.charAt(i) == '/' ){
            index = i;
            return givenUrl.substring(index+1);
         }
      }
      return null;
   }
   public int getNumberOfCSSLinks(){
      String source = this.getPageContents();
      int lengthOfTheURL = source.length();
      int numberOfCSSLinks = 0;
      int indexOfTheEndingParenthesis = 0;
//      
//      for( int startingPoint = 0; startingPoint < lengthOfTheURL; startingPoint++ ){
//         for( int endingPoint = lengthOfTheURL-1; endingPoint > startingPoint; endingPoint-- ){
//            
//            if( source.substring( startingPoint, endingPoint ).equals( "<link rel=\"stylesheet\"" ) ){
//               
//               for( int i = startingPoint; source.charAt(i) != '>'; i++)
//                  indexOfTheEndingParenthesis = i+1;
//               
//               for( int k = startingPoint; k+4 < indexOfTheEndingParenthesis; k++ )
//                  if( source.substring( k, k+5 ).equals( ".css\"" ) )
//                        numberOfCSSLinks++;
//            }
//            
//         }
//         
//      }
//      
//      return numberOfCSSLinks;
      
      for( int i = 0; i < lengthOfTheURL-5; i++ ) //html substringlerini kontrol et,
   }
}